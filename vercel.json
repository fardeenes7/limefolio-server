{
    "builds": [
        {
            "src": "limefolio/wsgi.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/static/(.*)",
            "dest": "/staticfiles/$1"
        },
        {
            "src": "/(.*)",
            "dest": "limefolio/wsgi.py"
        }
    ],
    "env": {
        "DJANGO_SETTINGS_MODULE": "limefolio.settings"
    }
}
