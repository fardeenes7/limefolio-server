"""
Dashboard API URLs - Requires OAuth2 Bearer Token
For authenticated users managing their portfolio
"""
from django.urls import path

# Placeholder - will be populated with actual endpoints later
urlpatterns = [
    # Example: path('projects/', ProjectListCreateView.as_view(), name='dashboard-projects'),
]
