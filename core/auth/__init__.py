"""
Authentication module for portfolio app.
Includes authentication classes, permissions, and middleware.
"""
# Note: Import these directly from submodules to avoid circular imports
# e.g., from portfolio.auth.authentication import APIKeyAuthentication
